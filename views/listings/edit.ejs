<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WanderLust</title>
</head>
<body>
    <h3>Edit Listing</h3>
    <form method="post" action="/listings/<%= listing.id %>?_method=PUT">
        <input name="listing[title]" value="<%= listing.title %>" placeholder="Enter the title" type="text">
        <br></br>
        <textarea name="listing[description]" placeholder="Enter a description" type="text"><%= listing.description %></textarea>
        <br></br>
        <input name="listing[image]" value="<%= listing.image.url %>" type="text">
        <br></br>
        <input name="listing[price]" value="<%= listing.price %>" placeholder="Enter the price" type="number">
        <br></br>
        <input name="listing[location]" value="<%= listing.location %>" placeholder="Enter the location" type="text">
        <br></br>
        <input name="listing[country]" value="<%= listing.country %>" placeholder="Enter the country" type="text">
        <br></br>
        <button>Update</button>
    </form>
</body>
</html>